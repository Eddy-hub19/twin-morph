# Продакшн-адрес NestJS co-op сервера. Не секрет (публичный URL, нужен
# браузеру напрямую для Socket.IO), поэтому коммитится — в отличие от
# .env.local (см. .gitignore: игнорируется только *.local).
NEXT_PUBLIC_SOCKET_URL=https://twin-morph.onrender.com
