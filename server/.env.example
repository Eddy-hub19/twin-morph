# Адрес фронтенда (Next.js) — единственный разрешённый CORS/Socket.IO origin.
FRONTEND_URL=https://twin-morph.vercel.app

# Render сам подставляет свой PORT в окружение — переопределять его тут
# для деплоя не нужно, значение по умолчанию (3001) только для локального run.
PORT=3001
